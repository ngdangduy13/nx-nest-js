name: currency-cron
on:
  schedule:
    - cron: '*/2 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our Currency API route
        run: |
          curl --location --request PUT 'https://api-asd123.vercel.app/api/currency'
